apiVersion: v1
kind: Service
metadata:
  name: voodle-service
spec:
  selector:
    app: voodle 
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 3000
    nodePort: 31110
